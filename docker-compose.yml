version: '3.8'

services:
  sitemap:
    build: .
    container_name: sitemap-generator
    ports:
      - "1111:1111"
    environment:
      - API_URL=https://bk.afd-platform.uz/afd-platform/backend/urls/all-movies/all
      - PORT=1111
    restart: unless-stopped